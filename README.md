# 实验四:简单聊天程序的设计与实现

计算机网络综合课设 · 实验四。在老师给定的参考程序(Java Socket 聊天工具骨架)基础上,完成文档 4.2 的全部功能要求并修复参考程序的 13 处缺陷。

## 功能

对照课设文档 4.2 功能列表,全部完成:

| 文档要求 | 状态 |
|---|---|
| ①客户端界面显示所有联入用户(服务器实时推送) | ✅ |
| ②两个用户之间的私聊(双击用户名发起,[私聊]标记+回显) | ✅ |
| ③增加好友 + 好友上线提醒 | ✅ |
| ④个人信息修改、保存、查询(服务器端持久化) | ✅ |
| ⑤聊天内容支持中文(重写收发编码为 UTF-8) | ✅ |
| ⑥聊天内容支持图片(聊天区内嵌显示) | ✅ |
| ⑦用户之间传输文件(Base64 走消息通道,限 5MB) | ✅ |
| ⑧其他扩展:参考程序缺陷分析与修复 13 处 | ✅ |

详细的实现方案、缺陷分析表和验证记录见 [功能完成清单.md](功能完成清单.md)。

## 编译与运行

需要 JDK 8+(在 JDK 17 上开发测试)。**源码为 UTF-8 编码,必须带 `-encoding UTF-8` 编译**:

```powershell
# 编译(在仓库根目录)
javac -encoding UTF-8 -d out (Get-ChildItem -Recurse com -Filter *.java).FullName

# 启动服务器(默认端口 3500,被占用会自动顺延)
java -cp out com.cncd.ch04.server.MainServer 3500

# 启动客户端(可开多个)
java -cp out com.cncd.ch04.client.ChatClient
```

客户端连接后常用命令:`/nick 名字`、`/users`、`/msg 名字 内容`、`/register 名字 密码`、`/setinfo 字段 内容`、`/getinfo [用户]`、`/addfriend 名字`、`/friends`;发文件/图片用界面上的"文件"按钮(右侧列表选中=私发,不选=群发)。

## 局域网多机部署

1. 运行 `build-jar.bat` 生成 `chat-server.jar` 与 `chat-client.jar`(目标机只需 JRE);
2. 服务器机器双击 `chat-server.jar`(或 `java -jar chat-server.jar 3500`),首次运行在防火墙弹窗点"允许",窗口会打印本机全部局域网 IP;
3. 其他机器拷贝 `chat-client.jar` 双击运行,点 **"扫描局域网"**(UDP 广播自动发现服务器并填入地址),再点 Connect 即可;
4. 全部消息默认 **AES 加密**传输;两端都加 `-Dchat.plain=true` 启动可切回明文(抓包对比演示用)。

## 服务器启动参数(阶段二)

```
java -jar chat-server.jar 3500 [可选参数]
  -Dchat.admin=<昵称>   指定管理员(可用 /kick /mute /unmute /announce)
  -Dchat.pass=<口令>    设置进入口令(客户端连接后需输入口令才能聊天)
  -Dchat.plain=true     关闭 AES 加密(明文,供抓包对比)
```

阶段二命令:`/join <群>` `/part <群>` `/rooms`(群组)、`/kick` `/mute` `/unmute` `/announce`(管理员)、`/enter <口令>`(准入)。客户端另有:左侧"＋群组"按钮、底部网络状态栏(RTT/丢包/流量/在线时长)、私聊已送达·已读、对方"正在输入"、自己消息的"撤回"链接(2 分钟内)。

## 协议约定

单 TCP 连接,消息以字节 `0xFF` 结尾;`0xFD` 开头表示客户端命令;`0x01` 开头表示服务器推送(在线名单 `USERLIST`、文件 `FILE`);正文统一 UTF-8 编码(三个控制字节均不会出现在合法 UTF-8 序列中,天然无冲突)。

## 目录说明

- `com/cncd/ch04/server/` — 服务器:监听与连接池、每连接收发双线程、命令解析、文件数据源;
- `com/cncd/ch04/client/` — 客户端:Swing 界面 + 网络内核(收发双线程);
- `原始版本备份/` — 老师下发的原始参考程序,未做任何修改,供对照;
- `功能完成清单.md` — 功能实现与缺陷修复的完整记录(工作量汇报底稿)。

服务器数据落盘于运行用户主目录 `.mihalychat\`(注册用户、个人信息、好友关系);客户端接收的文件保存于用户主目录 `ChatDownloads\`。
